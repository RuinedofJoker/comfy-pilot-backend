# Claude Skills 完整指南

> 本文档全面总结了 Claude Skills 的核心概念、架构设计、API 使用、最佳实践等内容，不涉及具体编程语言框架的实现细节。

---

## 📋 目录

1. [核心概念](#核心概念)
2. [Skills 架构](#skills-架构)
3. [内置技能](#内置技能)
4. [自定义技能开发](#自定义技能开发)
5. [API 规范](#api-规范)
6. [文件管理](#文件管理)
7. [应用场景](#应用场景)
8. [最佳实践](#最佳实践)
9. [性能优化](#性能优化)
10. [安全考虑](#安全考虑)
11. [故障排查](#故障排查)

---

## 核心概念

### 什么是 Skills？

**Skills（技能）** 是 Claude 的一项核心功能，可以理解为：

> **有组织的指令包、可执行代码和资源的集合，赋予 Claude 特定领域的专业能力。**

### 核心特征

#### 1. **专业能力包**
- 将领域知识、操作流程、代码逻辑封装成独立单元
- 类似于"专家系统"，让 Claude 在特定任务上表现出专业水平
- 可复用、可共享、可版本控制

#### 2. **动态加载机制**
- Skills 不会一次性全部加载到上下文中
- 根据用户请求动态发现和加载相关技能
- 按需加载，优化 token 使用效率

#### 3. **渐进式披露架构**
- **第一阶段**：加载技能的基本描述和元数据
- **第二阶段**：根据需要加载详细指令
- **第三阶段**：执行具体的代码或操作
- **优势**：最小化初始 token 消耗，提高响应速度

#### 4. **沙箱执行环境**
- Skills 在隔离的云端环境中执行
- 确保安全性，防止恶意代码影响系统
- 由 Anthropic 管理基础设施和运行时

### Skills 的价值

✅ **效率提升** - 自动化复杂任务，减少人工操作
✅ **一致性保证** - 标准化流程，确保输出质量
✅ **知识封装** - 将专业知识固化为可复用组件
✅ **扩展能力** - 突破纯文本对话限制，生成实际文件
✅ **团队协作** - 工作区级别共享，促进知识传播

---

## Skills 架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    用户请求                              │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Claude 核心推理引擎                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  1. 意图识别 → 2. 技能发现 → 3. 技能选择        │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                 Skills 容器                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 内置技能     │  │ 自定义技能   │  │ 第三方技能   │  │
│  │ - xlsx       │  │ - 财务分析   │  │ - 行业专用   │  │
│  │ - pptx       │  │ - 品牌指南   │  │ - 企业定制   │  │
│  │ - pdf        │  │ - 建模套件   │  │              │  │
│  │ - docx       │  │              │  │              │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              代码执行引擎                                │
│  - Python 运行时                                        │
│  - 沙箱隔离环境                                         │
│  - 文件系统访问                                         │
│  - 第三方库支持                                         │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Files API（文件存储）                       │
│  - 临时存储（24小时）                                   │
│  - 文件元数据管理                                       │
│  - 下载接口                                             │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  用户获取文件                            │
└─────────────────────────────────────────────────────────┘
```

### 技能生命周期

```
创建 → 上传 → 验证 → 激活 → 使用 → 更新 → 版本管理 → 删除
  │      │      │      │      │      │        │          │
  │      │      │      │      │      │        │          └─ 清理资源
  │      │      │      │      │      │        └─ 版本控制
  │      │      │      │      │      └─ 迭代改进
  │      │      │      │      └─ 动态加载执行
  │      │      │      └─ 工作区可用
  │      │      └─ 结构和内容检查
  │      └─ 提交到 Anthropic
  └─ 定义 SKILL.md
```

### 技能组成结构

```
my_skill/
├── SKILL.md              # 必需：技能定义文件
│   ├── YAML 前置元数据   # name, description
│   └── Markdown 正文     # 详细说明和指令
│
├── REFERENCE.md          # 可选：参考文档
│   └── 详细的背景知识、API 文档、示例等
│
├── scripts/              # 可选：可执行脚本
│   ├── processor.py      # Python 脚本
│   ├── analyzer.py
│   └── utils.py
│
└── resources/            # 可选：资源文件
    ├── templates/        # 模板文件
    │   └── report_template.xlsx
    ├── data/             # 数据文件
    │   └── industry_benchmarks.json
    └── config/           # 配置文件
        └── settings.yaml
```

---

## 内置技能

### 官方预构建技能

Anthropic 提供了 4 个开箱即用的内置技能：

#### 1. **Excel 技能（xlsx）**

**功能：**
- 创建和操作 Excel 工作簿
- 支持多工作表
- 公式和函数计算
- 图表生成（柱状图、折线图、饼图等）
- 数据透视表
- 条件格式化
- 单元格样式和格式

**典型用例：**
- 财务报表生成
- 数据分析仪表板
- 预算和预测模型
- 库存管理表
- 销售报告

**技术规格：**
- 格式：`.xlsx`（Office Open XML）
- 最大工作表数：无限制
- 支持的图表类型：15+ 种
- 公式支持：Excel 标准函数库

#### 2. **PowerPoint 技能（pptx）**

**功能：**
- 生成专业演示文稿
- 自定义幻灯片布局
- 插入图表和图形
- 文本格式化
- 幻灯片过渡效果
- 母版设计
- 嵌入图像和媒体

**典型用例：**
- 业务汇报演示
- 产品发布会
- 培训材料
- 季度总结
- 投资者路演

**技术规格：**
- 格式：`.pptx`（Office Open XML）
- 幻灯片数量：无限制
- 支持的布局：标题、内容、图表、空白等
- 图表集成：与 Excel 数据联动

#### 3. **PDF 技能（pdf）**

**功能：**
- 创建格式化 PDF 文档
- 文本排版和样式
- 表格和列表
- 图像嵌入
- 页眉和页脚
- 目录生成
- 超链接和书签

**典型用例：**
- 正式报告
- 合同和协议
- 用户手册
- 白皮书
- 证书和凭证

**技术规格：**
- 格式：`.pdf`（PDF 1.7）
- 页面大小：A4、Letter、自定义
- 字体嵌入：支持
- 加密：可选

#### 4. **Word 技能（docx）**

**功能：**
- 生成 Word 文档
- 富文本格式化
- 标题和样式
- 表格和列表
- 图像插入
- 页面布局
- 节和分页

**典型用例：**
- 业务文档
- 技术规范
- 会议纪要
- 提案和计划
- 标准操作程序

**技术规格：**
- 格式：`.docx`（Office Open XML）
- 样式：标题 1-9、正文、引用等
- 表格：支持合并单元格、边框样式
- 图像：PNG、JPEG 嵌入

### 内置技能使用方式

**API 调用结构：**

```json
{
  "model": "claude-sonnet-4-5-20250929",
  "max_tokens": 4096,
  "container": {
    "skills": [
      {
        "type": "anthropic",
        "skill_id": "xlsx",
        "version": "latest"
      }
    ]
  },
  "tools": [
    {
      "type": "code_execution_20250825",
      "name": "code_execution"
    }
  ],
  "messages": [
    {
      "role": "user",
      "content": "创建一个包含销售数据的 Excel 报表"
    }
  ]
}
```

**关键要素：**
1. `container.skills` - 指定要加载的技能
2. `tools` - 必须包含 `code_execution` 工具
3. `type: "anthropic"` - 表示使用内置技能
4. `version: "latest"` - 使用最新版本

---

## 自定义技能开发

### SKILL.md 文件规范

**SKILL.md** 是自定义技能的核心定义文件，必须遵循特定格式。

#### 基本结构

```markdown
---
name: skill-name
description: 简短描述技能的功能（建议 100-200 字符）
---

# 技能标题

## 功能概述
详细描述技能的能力和用途

## 使用方法
说明如何使用这个技能

## 输入格式
定义期望的输入数据格式

## 输出格式
说明技能的输出结果

## 示例
提供使用示例

## 限制和注意事项
说明技能的局限性
```

#### YAML 前置元数据规范

**必需字段：**
- `name`: 技能名称（小写字母、数字、连字符，如 `financial-analyzer`）
- `description`: 简短描述（最多 1024 字符）

**命名规则：**
- 使用小写字母
- 单词间用连字符分隔
- 避免特殊字符
- 保持简洁明了

**示例：**
```yaml
---
name: financial-ratio-calculator
description: 计算和解释关键财务比率，包括盈利能力、流动性、杠杆和效率指标
---
```

### 技能开发最佳实践

#### 1. **清晰的功能定义**

✅ **好的示例：**
```markdown
## 功能
本技能计算以下财务比率：
- 盈利能力：ROE、ROA、净利率
- 流动性：流动比率、速动比率
- 杠杆：债务权益比、利息覆盖率
```

❌ **不好的示例：**
```markdown
## 功能
这个技能可以做很多财务分析相关的事情。
```

#### 2. **结构化的输入输出定义**

✅ **好的示例：**
```markdown
## 输入格式
JSON 格式：
{
  "revenue": 1000000,
  "netIncome": 150000,
  "totalAssets": 500000,
  "totalEquity": 300000
}
```

❌ **不好的示例：**
```markdown
## 输入
提供财务数据即可。
```

#### 3. **实用的示例**

```markdown
## 使用示例

**示例 1：基本比率计算**
输入："计算 ROE，净利润 15 万，股东权益 30 万"
输出：ROE = 50%

**示例 2：完整分析**
输入：附加的财务报表 CSV 文件
输出：包含所有比率的 Excel 分析报告
```

### 技能目录大小限制

**硬性限制：**
- 总大小：最大 **8MB**
- YAML 前置元数据：最大 **1024 字符**
- 文件数量：建议不超过 **50 个**

**优化建议：**
- 压缩大型数据文件
- 使用外部 API 而非嵌入大量数据
- 精简脚本代码
- 移除不必要的依赖

### REFERENCE.md 使用指南

**REFERENCE.md** 用于存储详细的参考信息，不会在初始加载时消耗 token。

**适合放入 REFERENCE.md 的内容：**
- 详细的 API 文档
- 行业标准和规范
- 大量的示例代码
- 技术背景知识
- 数据字典和术语表

**示例结构：**
```markdown
# 财务比率参考文档

## 盈利能力比率详解

### ROE（净资产收益率）
**公式：** ROE = 净利润 / 股东权益
**行业基准：**
- 优秀：> 20%
- 良好：15-20%
- 一般：10-15%
- 较差：< 10%

### ROA（总资产收益率）
**公式：** ROA = 净利润 / 总资产
...
```

### 脚本开发规范

**scripts/ 目录** 用于存放可执行的 Python 脚本。

#### 脚本命名约定

```
scripts/
├── calculate_ratios.py      # 动词开头，描述功能
├── validate_data.py
├── generate_report.py
└── utils.py                  # 通用工具函数
```

#### 脚本编写原则

1. **单一职责** - 每个脚本专注一个功能
2. **清晰的函数签名** - 明确的输入输出
3. **错误处理** - 优雅地处理异常情况
4. **文档字符串** - 为函数添加说明
5. **可测试性** - 便于单元测试

#### 脚本示例

```python
"""
财务比率计算模块
"""

def calculate_roe(net_income: float, equity: float) -> float:
    """
    计算净资产收益率（ROE）

    Args:
        net_income: 净利润
        equity: 股东权益

    Returns:
        ROE 百分比值

    Raises:
        ValueError: 当股东权益为零时
    """
    if equity == 0:
        raise ValueError("股东权益不能为零")

    return (net_income / equity) * 100


def calculate_current_ratio(current_assets: float,
                           current_liabilities: float) -> float:
    """
    计算流动比率

    Args:
        current_assets: 流动资产
        current_liabilities: 流动负债

    Returns:
        流动比率
    """
    if current_liabilities == 0:
        return float('inf')

    return current_assets / current_liabilities
```

### 资源文件管理

**resources/ 目录** 用于存放模板、数据、配置等资源文件。

#### 推荐的目录结构

```
resources/
├── templates/              # 模板文件
│   ├── excel/
│   │   └── financial_report_template.xlsx
│   ├── powerpoint/
│   │   └── presentation_template.pptx
│   └── pdf/
│       └── report_template.pdf
│
├── data/                   # 数据文件
│   ├── industry_benchmarks.json
│   ├── exchange_rates.csv
│   └── tax_rates.yaml
│
└── config/                 # 配置文件
    ├── settings.yaml
    └── constants.json
```

#### 资源文件最佳实践

1. **使用相对路径** - 确保跨环境可移植
2. **版本控制** - 为数据文件添加版本标识
3. **文档说明** - 为每个资源文件添加 README
4. **格式选择** - JSON/YAML 用于配置，CSV 用于表格数据

---

## API 规范

### Beta API Headers

Skills 功能需要特定的 Beta headers：

```http
anthropic-beta: code-execution-2025-08-25,files-api-2025-04-14,skills-2025-10-02
```

**各 header 的作用：**
- `code-execution-2025-08-25` - 启用代码执行能力
- `files-api-2025-04-14` - 启用文件 API
- `skills-2025-10-02` - 启用 Skills 功能

### Skills API 端点

#### 1. 创建技能

**端点：** `POST /v1/skills`

**请求体：**
```json
{
  "display_title": "Financial Analyzer",
  "files": [
    {
      "name": "SKILL.md",
      "content": "base64_encoded_content"
    },
    {
      "name": "scripts/calculate.py",
      "content": "base64_encoded_content"
    }
  ]
}
```

**响应：**
```json
{
  "id": "skill_abc123xyz",
  "display_title": "Financial Analyzer",
  "latest_version": "v1",
  "created_at": "2026-01-30T10:00:00Z",
  "source": "custom"
}
```

#### 2. 列出技能

**端点：** `GET /v1/skills?source=custom`

**查询参数：**
- `source`: `custom` | `anthropic` | `all`（默认：`all`）
- `limit`: 每页数量（默认：20）
- `after`: 分页游标

**响应：**
```json
{
  "data": [
    {
      "id": "skill_abc123",
      "display_title": "Financial Analyzer",
      "latest_version": "v2",
      "created_at": "2026-01-30T10:00:00Z",
      "updated_at": "2026-01-30T15:00:00Z"
    }
  ],
  "has_more": false
}
```

#### 3. 获取技能详情

**端点：** `GET /v1/skills/{skill_id}`

**响应：**
```json
{
  "id": "skill_abc123",
  "display_title": "Financial Analyzer",
  "latest_version": "v2",
  "created_at": "2026-01-30T10:00:00Z",
  "updated_at": "2026-01-30T15:00:00Z",
  "source": "custom"
}
```

#### 4. 创建技能版本

**端点：** `POST /v1/skills/{skill_id}/versions`

**请求体：**
```json
{
  "files": [
    {
      "name": "SKILL.md",
      "content": "base64_encoded_content"
    }
  ]
}
```

**响应：**
```json
{
  "version": "v3",
  "skill_id": "skill_abc123",
  "created_at": "2026-01-30T16:00:00Z"
}
```

#### 5. 列出技能版本

**端点：** `GET /v1/skills/{skill_id}/versions`

**响应：**
```json
{
  "data": [
    {
      "version": "v3",
      "skill_id": "skill_abc123",
      "created_at": "2026-01-30T16:00:00Z"
    },
    {
      "version": "v2",
      "skill_id": "skill_abc123",
      "created_at": "2026-01-30T15:00:00Z"
    }
  ]
}
```

#### 6. 获取版本详情

**端点：** `GET /v1/skills/{skill_id}/versions/{version}`

**响应：**
```json
{
  "version": "v2",
  "skill_id": "skill_abc123",
  "name": "financial-analyzer",
  "description": "分析财务报表并计算关键比率",
  "directory": {
    "SKILL.md": "content...",
    "scripts/calculate.py": "content..."
  },
  "created_at": "2026-01-30T15:00:00Z"
}
```

#### 7. 删除技能版本

**端点：** `DELETE /v1/skills/{skill_id}/versions/{version}`

**响应：** `204 No Content`

#### 8. 删除技能

**端点：** `DELETE /v1/skills/{skill_id}`

**注意：** 必须先删除所有版本才能删除技能本身。

**响应：** `204 No Content`

### Messages API 与 Skills 集成

#### 使用技能的完整请求

```json
{
  "model": "claude-sonnet-4-5-20250929",
  "max_tokens": 4096,
  "container": {
    "skills": [
      {
        "type": "anthropic",
        "skill_id": "xlsx",
        "version": "latest"
      },
      {
        "type": "custom",
        "skill_id": "skill_abc123",
        "version": "v2"
      }
    ]
  },
  "tools": [
    {
      "type": "code_execution_20250825",
      "name": "code_execution"
    }
  ],
  "messages": [
    {
      "role": "user",
      "content": "使用财务分析技能分析这些数据，并生成 Excel 报表"
    }
  ]
}
```

#### 响应结构

```json
{
  "id": "msg_abc123",
  "type": "message",
  "role": "assistant",
  "content": [
    {
      "type": "text",
      "text": "我已经分析了财务数据并生成了报表..."
    },
    {
      "type": "tool_use",
      "id": "toolu_xyz789",
      "name": "code_execution",
      "input": {
        "code": "# Python code..."
      }
    }
  ],
  "model": "claude-sonnet-4-5-20250929",
  "stop_reason": "end_turn",
  "usage": {
    "input_tokens": 1250,
    "output_tokens": 850
  }
}
```

#### 工具结果中的文件引用

```json
{
  "type": "tool_result",
  "tool_use_id": "toolu_xyz789",
  "content": [
    {
      "type": "text",
      "text": "已生成财务报表"
    },
    {
      "type": "file",
      "file_id": "file_def456",
      "filename": "financial_report.xlsx"
    }
  ]
}
```

---

## 文件管理

### Files API 概述

Skills 生成的文件通过 **Files API** 进行管理。

#### 文件生命周期

```
生成 → 临时存储 → 下载 → 24小时后自动删除
  │        │         │              │
  │        │         │              └─ 自动清理
  │        │         └─ 用户获取文件内容
  │        └─ Anthropic 服务器（24小时）
  └─ Skills 执行生成
```

### Files API 端点

#### 1. 下载文件

**端点：** `GET /v1/files/{file_id}/content`

**响应：** 二进制文件内容（application/octet-stream）

**示例：**
```http
GET /v1/files/file_abc123/content
Authorization: Bearer YOUR_API_KEY
anthropic-beta: files-api-2025-04-14
```

#### 2. 获取文件元数据

**端点：** `GET /v1/files/{file_id}`

**响应：**
```json
{
  "id": "file_abc123",
  "filename": "financial_report.xlsx",
  "size_bytes": 45678,
  "mime_type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
  "created_at": "2026-01-30T10:00:00Z",
  "type": "output",
  "downloadable": true
}
```

**字段说明：**
- `size_bytes` - 文件大小（字节）
- `mime_type` - MIME 类型
- `type` - 文件类型（`output` 表示 Skills 生成）
- `downloadable` - 是否可下载

#### 3. 列出文件

**端点：** `GET /v1/files`

**查询参数：**
- `limit` - 每页数量（默认：20）
- `after` - 分页游标

**响应：**
```json
{
  "data": [
    {
      "id": "file_abc123",
      "filename": "report.xlsx",
      "size_bytes": 45678,
      "created_at": "2026-01-30T10:00:00Z"
    }
  ],
  "has_more": false
}
```

#### 4. 删除文件

**端点：** `DELETE /v1/files/{file_id}`

**响应：** `204 No Content`

**注意：** 文件会在 24 小时后自动删除，手动删除是可选的。

### 文件处理最佳实践

#### 1. 立即下载

```
生成文件 → 立即提取 file_id → 立即下载 → 保存到本地
```

**原因：**
- 避免 24 小时过期
- 减少网络请求失败风险
- 确保文件可用性

#### 2. 错误处理

```
尝试下载
  ↓
成功？
  ├─ 是 → 保存文件
  └─ 否 → 重试（最多3次）
           ↓
         仍失败？
           ├─ 是 → 记录错误，通知用户
           └─ 否 → 保存文件
```

#### 3. 文件验证

下载后验证文件完整性：
- 检查文件大小是否匹配
- 验证 MIME 类型
- 尝试打开文件确认格式正确

#### 4. 本地存储策略

```
outputs/
├── 2026-01-30/              # 按日期组织
│   ├── financial_report_001.xlsx
│   ├── presentation_002.pptx
│   └── summary_003.pdf
└── archive/                 # 归档旧文件
    └── 2026-01/
```

---

## 应用场景

### 1. 财务报告自动化

**场景描述：**
每月生成标准化的财务报表，包括损益表、资产负债表、现金流量表。

**技能组合：**
- 自定义技能：`financial-analyzer`（计算比率）
- 内置技能：`xlsx`（生成 Excel）
- 内置技能：`pptx`（生成演示文稿）

**工作流：**
```
原始数据（CSV/JSON）
  ↓
财务分析技能（计算比率、趋势分析）
  ↓
Excel 技能（生成详细报表）
  ↓
PowerPoint 技能（生成管理层汇报）
  ↓
PDF 技能（生成正式报告）
```

**优势：**
- 节省 80% 的手工时间
- 确保计算准确性
- 格式统一专业
- 可追溯和审计

### 2. 企业品牌一致性

**场景描述：**
确保所有对外文档符合企业品牌指南。

**技能组合：**
- 自定义技能：`brand-guidelines`（应用品牌标准）
- 内置技能：`pptx`、`docx`、`pdf`

**应用示例：**
- 销售提案自动套用品牌模板
- 市场材料统一视觉风格
- 内部文档标准化格式

**价值：**
- 品牌形象一致
- 减少设计审核时间
- 提升专业度

### 3. 数据分析和可视化

**场景描述：**
将原始数据转换为可视化的分析报告。

**技能组合：**
- 内置技能：`xlsx`（数据处理和图表）
- 内置技能：`pptx`（可视化展示）

**典型流程：**
```
原始数据 → 清洗和转换 → 统计分析 → 图表生成 → 报告输出
```

**应用领域：**
- 销售业绩分析
- 市场趋势报告
- 运营指标仪表板
- 客户行为分析

### 4. 投资分析和建模

**场景描述：**
构建复杂的财务模型进行投资决策。

**技能组合：**
- 自定义技能：`financial-modeling`（DCF、敏感性分析）
- 内置技能：`xlsx`（模型构建）

**应用：**
- 企业估值
- 项目可行性分析
- 并购建模
- 风险评估

### 5. 合规和审计报告

**场景描述：**
生成符合监管要求的标准化报告。

**技能组合：**
- 自定义技能：`compliance-checker`（合规验证）
- 内置技能：`pdf`（正式报告）
- 内置技能：`docx`（详细文档）

**特点：**
- 确保合规性
- 可审计追溯
- 标准化格式
- 自动化流程

---

## 最佳实践

### 技能设计原则

#### 1. 单一职责原则

✅ **好的设计：**
```
financial-ratio-calculator    # 只计算财务比率
report-generator             # 只生成报告
data-validator               # 只验证数据
```

❌ **不好的设计：**
```
financial-everything         # 做所有财务相关的事
```

#### 2. 清晰的接口定义

**输入输出明确：**
```markdown
## 输入
- 格式：JSON
- 必需字段：revenue, expenses, net_income
- 可选字段：previous_year_data

## 输出
- 格式：JSON
- 字段：ratios (object), analysis (string), recommendations (array)
```

#### 3. 错误处理和验证

**在 SKILL.md 中说明：**
```markdown
## 错误处理
- 缺少必需字段 → 返回错误信息
- 数据格式错误 → 提供格式示例
- 计算异常（如除零）→ 返回特殊值或警告
```

#### 4. 版本管理策略

**语义化版本：**
- `v1.0` - 初始版本
- `v1.1` - 小功能增强
- `v2.0` - 重大变更

**变更日志：**
```markdown
## 版本历史

### v2.0 (2026-01-30)
- 新增：支持多币种计算
- 变更：修改输出格式为 JSON
- 修复：除零错误处理

### v1.1 (2026-01-15)
- 新增：行业基准对比
- 优化：计算性能提升 50%
```

#### 5. 文档完整性

**必需文档：**
- ✅ SKILL.md - 核心定义
- ✅ 使用示例 - 至少 3 个
- ✅ 输入输出规范 - 详细说明
- ✅ 错误处理 - 常见问题

**可选但推荐：**
- 📄 REFERENCE.md - 详细参考
- 📄 CHANGELOG.md - 版本历史
- 📄 README.md - 快速开始

### 技能组合策略

#### 1. 技能链式调用

```
技能A（数据清洗）→ 技能B（分析计算）→ 技能C（报告生成）
```

**示例：**
```
data-validator → financial-analyzer → xlsx
```

#### 2. 技能并行使用

```
        ┌─ 技能A（Excel报表）
数据 ───┼─ 技能B（PPT演示）
        └─ 技能C（PDF报告）
```

#### 3. 技能嵌套组合

```
外层技能（工作流编排）
  ├─ 内层技能1（数据处理）
  ├─ 内层技能2（分析）
  └─ 内层技能3（输出）
```

### 性能优化建议

#### 1. 最小化技能大小

- 移除不必要的文件
- 压缩大型资源
- 使用外部 API 替代嵌入数据

#### 2. 优化 SKILL.md 内容

- 简洁的描述（避免冗长）
- 结构化的指令（使用列表和表格）
- 关键信息前置

#### 3. 利用 REFERENCE.md

将详细内容放入 REFERENCE.md：
- 详细的技术文档
- 大量的示例代码
- 背景知识和理论

#### 4. 脚本优化

```python
# ✅ 好的实践
def calculate_ratio(a: float, b: float) -> float:
    """快速计算比率"""
    return a / b if b != 0 else 0.0

# ❌ 避免
def calculate_ratio(a, b):
    # 大量的日志输出
    # 复杂的错误处理
    # 不必要的计算
    pass
```

---

## 性能优化

### Token 使用优化

#### 1. 渐进式披露

**工作原理：**
```
第一次调用：加载技能元数据（~100 tokens）
  ↓
需要详细信息：加载 SKILL.md 主体（~500 tokens）
  ↓
需要参考资料：加载 REFERENCE.md（~2000 tokens）
```

**优势：**
- 减少不必要的 token 消耗
- 加快响应速度
- 降低成本

#### 2. 技能选择优化

**让 Claude 智能选择：**
```json
{
  "container": {
    "skills": [
      {"type": "custom", "skill_id": "skill_1"},
      {"type": "custom", "skill_id": "skill_2"},
      {"type": "anthropic", "skill_id": "xlsx"}
    ]
  }
}
```

Claude 会根据用户请求自动选择最相关的技能。

#### 3. 缓存策略

**容器 ID 复用：**
```
第一次请求 → 创建容器 → 加载技能 → 返回 container_id
后续请求 → 使用 container_id → 跳过加载 → 直接执行
```

**节省：**
- 减少技能加载时间
- 降低 token 消耗
- 提高响应速度

### 执行时间优化

#### 1. 文档生成时间

**实际观测时间：**
- Excel 生成：1-2 分钟
- PowerPoint 生成：1-2 分钟
- PDF 生成：1-2 分钟
- Word 生成：1-2 分钟

**优化建议：**
- 保持示例简洁
- 避免过于复杂的格式
- 减少图表数量
- 优化数据量

#### 2. 批量操作

**单次对话处理多个文件：**
```
用户："生成 Q1、Q2、Q3 三个季度的报表"
  ↓
Claude 在一次对话中生成 3 个文件
  ↓
比分 3 次对话更高效
```

#### 3. 异步处理

**对于大型任务：**
```
提交任务 → 返回任务 ID → 轮询状态 → 获取结果
```

---

## 安全考虑

### 数据安全

#### 1. 敏感数据处理

**原则：**
- ❌ 不要在 SKILL.md 中硬编码敏感信息
- ❌ 不要在脚本中包含 API 密钥
- ✅ 使用环境变量或参数传递
- ✅ 在文档中标注数据敏感性

**示例：**
```markdown
## 数据安全
本技能处理财务数据，请确保：
- 不要上传包含真实客户信息的数据
- 使用脱敏数据进行测试
- 生成的文件包含敏感信息，请妥善保管
```

#### 2. 文件存储安全

**24 小时临时存储：**
- ✅ 文件自动过期删除
- ✅ 仅创建者可访问
- ⚠️ 下载后立即从服务器删除（可选）

**本地存储：**
- 加密敏感文件
- 设置适当的文件权限
- 定期清理旧文件

#### 3. 访问控制

**工作区级别：**
- 技能在工作区内共享
- 仅工作区成员可访问
- 管理员可管理技能权限

### 代码安全

#### 1. 沙箱限制

**允许的操作：**
- ✅ 文件读写（沙箱内）
- ✅ 数据计算和处理
- ✅ 调用标准库

**禁止的操作：**
- ❌ 网络请求（除非明确允许）
- ❌ 系统命令执行
- ❌ 访问沙箱外的文件系统

#### 2. 依赖安全

**使用可信的库：**
```python
# ✅ 推荐
import pandas
import numpy
import openpyxl

# ⚠️ 谨慎使用
import requests  # 需要网络访问
import subprocess  # 可能有安全风险
```

#### 3. 输入验证

**在脚本中验证输入：**
```python
def validate_input(data: dict) -> bool:
    """验证输入数据"""
    required_fields = ['revenue', 'expenses']

    # 检查必需字段
    if not all(field in data for field in required_fields):
        return False

    # 检查数据类型
    if not isinstance(data['revenue'], (int, float)):
        return False

    # 检查数据范围
    if data['revenue'] < 0:
        return False

    return True
```

### 合规性考虑

#### 1. 数据隐私

**GDPR/CCPA 合规：**
- 明确说明数据使用方式
- 不存储个人身份信息
- 提供数据删除机制

#### 2. 审计追踪

**记录关键操作：**
- 技能使用时间
- 输入数据摘要
- 生成的文件类型
- 用户身份

#### 3. 许可和版权

**技能内容：**
- 确保有权使用所有资源
- 标注第三方内容来源
- 遵守开源许可协议

---

## 故障排查

### 常见问题

#### 1. 技能未加载

**症状：**
```
Error: Skill not found
```

**可能原因：**
- skill_id 错误
- 技能未上传成功
- 版本不存在

**解决方案：**
```
1. 验证 skill_id 是否正确
2. 检查技能是否在工作区中
3. 确认版本号（使用 "latest" 或具体版本）
4. 重新上传技能
```

#### 2. 代码执行失败

**症状：**
```
Error: Code execution failed
```

**可能原因：**
- 脚本语法错误
- 缺少依赖库
- 运行时异常

**解决方案：**
```
1. 检查脚本语法
2. 验证所需库是否可用
3. 添加错误处理代码
4. 查看详细错误信息
```

#### 3. 文件下载失败

**症状：**
```
Error: File not found
```

**可能原因：**
- file_id 错误
- 文件已过期（24小时）
- 网络问题

**解决方案：**
```
1. 验证 file_id 是否正确
2. 检查文件是否在有效期内
3. 重试下载（实现重试机制）
4. 重新生成文件
```

#### 4. Token 限制超出

**症状：**
```
Error: Token limit exceeded
```

**可能原因：**
- 技能内容过大
- 加载了过多技能
- 对话历史过长

**解决方案：**
```
1. 精简 SKILL.md 内容
2. 使用 REFERENCE.md 存储详细信息
3. 减少同时加载的技能数量
4. 清理对话历史
```

